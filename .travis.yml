language: c
sudo: false
addons:
  apt:
    sources:
    - avsm
    packages:
    - ocaml
    - ocaml-native-compilers
    - opam

script: bash -ex .travis-ci.sh
env:
  - OPAM_SWITCH=system
  - OPAM_SWITCH=4.03.0
  - OPAM_SWITCH=4.04.0
cache:
  directories:
  - $HOME/.opam
